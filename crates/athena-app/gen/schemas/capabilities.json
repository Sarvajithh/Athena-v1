{"default":{"identifier":"default","description":"Default capability for the main window. Grants the notification plugin's default permission set (its documented setup step) plus the window-control permissions the custom title bar (TitleBar.tsx) needs: decorations are off (tauri.conf.json), so minimize/maximize/close/drag are no longer handled by the OS chrome and must go through these explicitly-granted commands instead. Scoped to the single main window this app has.","local":true,"windows":["main"],"permissions":["notification:default","core:window:allow-minimize","core:window:allow-toggle-maximize","core:window:allow-close","core:window:allow-start-dragging","core:window:allow-is-maximized"]}}