{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. No capabilities file existed before this task; this one exists solely to grant the notification plugin's default permission set (its documented setup step), scoped to the single main window this app has.",
  "windows": ["main"],
  "permissions": ["notification:default"]
}
